# Copy this file to .env.local for local development (Next.js loads it automatically).
# On Vercel, set this in Project Settings → Environment Variables.
# This key is server-side only — it never appears in client bundles.
ANTHROPIC_API_KEY=your-api-key-here
